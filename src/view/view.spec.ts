test("", () => {
  /* outdated
  - run worldView.initialize();
  - get all elements of class "wire"
  - get all elements of class "player"
  - expect that there is a wire with location 1/1
  - expect that there is a wire with location 2/1
  - expect that there is a player with location 3/3
   */

  /*
- v1 given a world with a component of types player and the rest empty
- v2 given a world with components of types wire (2) and player and the rest empty
- get current world representation as an array of components
- ask components for it's type and location
- draw shapes according to type on canvas
 */

  //expect(world.find({id: '34'})).toBe(component2);
});